# Clients 로고 흐름

최종 사용자 선택: 두 줄 고정, 컬러 유지. 비교용 query JS 제거, 단색 필터 제거, 컬러 원본 복원. Mnet은 공식 투명 마스크에 원본 분홍색 적용. 320~2560px 두 줄·반복 이동·hover/focus·reduced-motion·이미지 12개 및 build/check 검증 통과. 공개 배포 전 상태.

## 최신 사용자 피드백 반영

어두운 배경으로 통일, 정지 버튼과 개업 전 문구 삭제. hover·키보드 focus 정지 및 reduced-motion 유지. Mnet 공식 투명본, 해보자 imagegen 투명 시안으로 변경. 공식 밝은색 로고 및 일부 CSS 흰색 단색 처리. 개발 서버 한 줄 `/#clients`, 두 줄 `/?clients=2#clients` 비교 가능. 두 줄 역시 왼쪽 이동이며 PC 창 너비 1080px로 반복 공백 방지.

양쪽 버전 320/375/768/1967/2560px 이미지 12개·가로 넘침 없음·실제 행 수·좌향 이동·반복 연결·hover/focus 정지·reduced-motion 검증 통과. 아직 사용자 선택 전, 커밋·배포 없음. 아래는 최초 구현 기록이다.

사용자 승인 범위: 기존 Services와 Studio 사이 배치에 RAVNUS·SEMICS 추가, 12개 로고 수집, 왼쪽 연속 이동 구현. 커밋·배포 제외.

구조: src/data/clients.ts에서 목록 관리, Clients.astro에서 동일 목록 두 벌을 CSS로 연속 이동. 복제 목록은 보조기술에서 숨긴다. 밝은 배경과 원본 색상 유지. 일시정지 버튼·hover 정지, reduced-motion에서는 복제를 숨기고 전체 목록을 줄바꿈한다. 새 의존성 없음.

- [x] 원본 이미지와 출처 기록
- [x] Clients 컴포넌트와 메인 삽입
- [x] build/check, 브라우저에서 이미지 12개·좌향 이동·정지·반복 경계·모바일·reduced-motion 검증

검증: 320/375/768/1440/2560px 가로 넘침 없음, 이미지 12개 로딩, 좌향 이동, 버튼·hover 정지, Enter 재생, 반복 경계 오차 0.046px, reduced-motion 정지·전체 목록 표시. 페이지 오류 0. scripts/check-clients-browser.js 재실행 가능.

로컬 미리보기: http://127.0.0.1:4460/#clients (dev 서버).
PC·모바일·전체 로고 이미지: G:/91_uniStudio/260925_웹사이트_클라이언트/.

일반 포트폴리오 정리 및 관리자 코드는 이번 범위 밖이다.
# 09-25 다크 컬러 시안 추가

사용자 승인: 참고 이미지 같은 여백, 컬러 심벌 유지, 어두운 글자만 밝게 표시, 두 줄 유지. 로컬 미리보기만 진행.

1. clients.ts에 글자 영역 좌표 추가, Clients.astro에서 원본 위에 SVG 알파 마스크로 밝은 글자 표시.
2. 로고 폭과 행 간격 정돈, 원본 파일 보존 및 출처 문서에 공식 자산과 표시 시안 구분.
3. build/check 및 PC·모바일의 넘침, 이동, reduced-motion 확인 후 미리보기 제공.
